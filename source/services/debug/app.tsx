import type { CrochetForBrowser } from "../../targets/browser";

export class DebugUI {
  render(crochet: CrochetForBrowser, root: HTMLElement) {
    console.warn("DebugUI is currently unavailable");
  }
}
