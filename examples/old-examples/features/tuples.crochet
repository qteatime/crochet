% crochet

command X id = X;

scene main do
  let List = [1, 2, 3, 4, 5, 6] shuffle;
  transcript inspect: (List);
  transcript inspect: (List sort: _ id);
  transcript inspect: (List sort);
  transcript inspect: (List sort reverse);
end