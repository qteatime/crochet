% crochet

command X id = X;

scene main do
  let Tuple = [1, 2, 3, 4, 5, 6] shuffle;
  transcript inspect: (Tuple);
  transcript inspect: (Tuple sort: _ id);
  transcript inspect: (Tuple sort);
  transcript inspect: (Tuple sort reverse);
end