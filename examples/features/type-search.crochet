% crochet

relation Direction* y-axis;
enum direction = north, east, south, west;

scene main do
  fact north y-axis;
  fact south y-axis;

  transcript inspect-line:
    (search Direction is direction,
            Direction y-axis,
            let N = (search _ is direction) count
    );
end
