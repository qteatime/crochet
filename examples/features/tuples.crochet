% crochet

command X id = X;

scene main do
  let Tuple = [1, 2, 3, 4, 5, 6] shuffle;
  transcript inspect-line: (Tuple);
  transcript inspect-line: (Tuple sort: _ id);
  transcript inspect-line: (Tuple sort);
  transcript inspect-line: (Tuple sort reverse);
end