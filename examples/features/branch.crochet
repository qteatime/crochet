% crochet

relation Who* at: Where;

singleton lielle { at: foyer; }
singleton karis { }

singleton foyer;

scene main {
  condition {
    when 1 > 2 { "1 > 2" }
    when 1 < 2 { "1 < 2" }
    always { "umpossible" }
  } inspect;

  match {
    when Who at: foyer { "[Who] at foyer" }
    always { "Nah" }
  } inspect;

  condition {
    when search karis at: foyer { "Nah" }
    always { "That's right" }
  } inspect;
}