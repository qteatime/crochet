% crochet

command X id = X;

scene main do
  let Stream = [1, 2, 3, 4, 5, 6] shuffle;
  transcript inspect-line: (Stream);
  transcript inspect-line: (Stream sort: _ id);
  transcript inspect-line: (Stream sort);
  transcript inspect-line: (Stream sort reverse);
end