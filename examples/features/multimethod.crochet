% crochet

command any other do
  "any other" inspect;
end

command any hello do
  "any hello" inspect;
end

command integer hello do
  "integer hello" inspect;
end

command partial hello do
  "partial hello" inspect;
end

command boolean hello do
  "boolean hello" inspect;
end

command true hello do
  "true hello" inspect;
end

scene main do
  1 other;
  1 hello;
  (_ + 1) hello;
  true hello;
  false hello;
end