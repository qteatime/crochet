% crochet

open crochet.ui.agata;

singleton app;

implement to-widget for app;
command app as widget do
  #widget flex-column: [
    layout-section,
    controls-button
  ]
  | with-padding: { X in X all: (2.0 as rem) }
end