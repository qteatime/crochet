{
  "private": true,
  "scripts": {
    "build": "dotnet fable Linguist.fsproj --outDir build --sourceMaps && webpack",
    "build-grammar": "fohm compile grammar/grammar.ohm --name Linguist --outDir source/generated"
  },
  "name": "linguist",
  "version": "1.0.0",
  "dependencies": {
    "@origamitower/fohm": "^1.1.0",
    "prettier": "^2.2.1",
    "webpack-cli": "^4.5.0",
    "yargs": "^12.0.5"
  }
}
