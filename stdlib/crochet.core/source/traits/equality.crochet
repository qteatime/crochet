% crochet

/// True if two values with an equality trait are not equal.
command (Left has equality) =/= (Right has equality) =
  not (Left === Right);


implement equality for nothing;

/// True if two nothings are equal
command nothing === nothing = true;


implement equality for not-found;

/// True if two not-founds are equal
command not-found === not-found = true;