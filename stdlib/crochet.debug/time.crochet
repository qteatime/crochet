% crochet

/// Minimal support for benchmarking Crochet code.
singleton benchmark;

/// Runs the given `Block` and outputs the amount of time it took to execute.
command benchmark block: (Block is function-0) tag: (Tag is text)
  = foreign debug.time(Tag, Block);

/// Runs the givne `Block` and outputs the amount of time it took to execute.
command benchmark block: Block =
  benchmark block: Block tag: "Time";
