% crochet

type widget-image(source is image) is widget;

command #widget image: (Source is image) =
  new widget-iamge(Source);

command #widget image: (Source is package-asset) =
  new widget-image(#image from-asset: Source);